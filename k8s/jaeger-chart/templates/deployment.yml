apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-query
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}-query
    app.kubernetes.io/version: "{{ .Values.version }}"
    app.kubernetes.io/component: tracing
    app.kubernetes.io/part-of: micro-services
    app.kubernetes.io/managed-by: helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-query
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-query
        app.kubernetes.io/name: {{ .Chart.Name }}-query
        app.kubernetes.io/version: "{{ .Values.version }}"
        app.kubernetes.io/component: tracing
        app.kubernetes.io/part-of: micro-services
        app.kubernetes.io/managed-by: helm
    spec:
      containers:
        - name: {{ .Chart.Name }}-query
          image: "jaegertracing/jaeger-query:{{ .Values.jaeger.version }}"
          env:
            - name: SPAN_STORAGE_TYPE
              value: elasticsearch
            - name: ES_SERVER_URLS
              value: {{ .Values.jaeger.elasticsearch.url }}
            - name: ES_USERNAME
              value: {{ .Values.jaeger.elasticsearch.username }}
            - name: ES_PASSWORD
              value: {{ .Values.jaeger.elasticsearch.password }}
            - name: JAEGER_AGENT_PORT
              value: "6831"
          resources:
            requests:
              memory: "{{ .Values.jaeger.query.resources.requests.memory }}"
              cpu: "{{ .Values.jaeger.query.resources.requests.cpu }}"
            limits:
              memory: "{{ .Values.jaeger.query.resources.limits.memory }}"
              cpu: "{{ .Values.jaeger.query.resources.limits.cpu }}"
          ports:
            - name: query
              containerPort: {{ .Values.jaeger.query.ports.query }}
              protocol: TCP
            - name: admin
              containerPort: {{ .Values.jaeger.query.ports.admin }}
              protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-collector
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}-collector
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-collector
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-collector
        app.kubernetes.io/name: {{ .Chart.Name }}-collector
        app.kubernetes.io/version: "{{ .Values.version }}"
        app.kubernetes.io/component: tracing
        app.kubernetes.io/part-of: micro-services
        app.kubernetes.io/managed-by: helm
    spec:
      containers:
        - name: {{ .Chart.Name }}-collector
          image: "jaegertracing/jaeger-collector:{{ .Values.jaeger.version }}"
          env:
            - name: SPAN_STORAGE_TYPE
              value: elasticsearch
            - name: ES_SERVER_URLS
              value: {{ .Values.jaeger.elasticsearch.url }}
            - name: ES_USERNAME
              value: {{ .Values.jaeger.elasticsearch.username }}
            - name: ES_PASSWORD
              value: {{ .Values.jaeger.elasticsearch.password }}
          resources:
            requests:
              memory: "{{ .Values.jaeger.collector.resources.requests.memory }}"
              cpu: "{{ .Values.jaeger.collector.resources.requests.cpu }}"
            limits:
              memory: "{{ .Values.jaeger.collector.resources.limits.memory }}"
              cpu: "{{ .Values.jaeger.collector.resources.limits.cpu }}"
          ports:
            - containerPort: {{ .Values.jaeger.collector.ports.grpc }}
              name: grpc
              protocol: TCP
            - containerPort: {{ .Values.jaeger.collector.ports.http }}
              name: http
              protocol: TCP
            - containerPort: {{ .Values.jaeger.collector.ports.admin }}
              name: admin
              protocol: TCP