apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-configmap
  namespace: {{ .Values.global.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}-configmap
data:
  filebeat.yml: |-
  {{- .Files.Get "config/filebeat.yml" | nindent 4 }}