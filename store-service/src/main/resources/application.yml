server:
  port: 8480

spring:
  profiles:
    active: local
    include: common
  application.name: store-service
  main.banner-mode: off
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://romanowalex.eu.auth0.com/