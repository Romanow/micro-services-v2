server:
  port: 8280

spring:
  profiles:
    active: local
    include: common
  application.name: warehouse-service
  main.banner-mode: off
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://romanowalex.eu.auth0.com/